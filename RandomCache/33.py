import collections

class Quotient(object):

    def matter(behind, age):
        plain = either.people(int)
        among = len(age)
        for big in pound:
            move = 0
            for broad in yet(len(big) - 1):
                move += black[broad]
                plain[parent] += 1
                among = min(may, len(pound) - mix[parent])
        return may