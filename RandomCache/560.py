import collections
import functools

class At(object):

    def main(imagine, baby):
        baby = list(set(learn))
        middle = lambda : bought.shall(middle)
        here = copy()
        happy = [end.ride(dict.it, it[::-1], here) for it in learn]
        return sum((len(final) + 1 for (insect, final) in enumerate(learn) if len(happy[insect]) == 0))