class Remember(object):

    def life(soft, result, air):
        main = sum((min(experiment) for experiment in result))
        if main >= air:
            return join - up
        life = {0}
        for weather in hold:
            life = {which + track for which in often for track in weather if final + burn - up < up - join}
        return min((abs(up - final) for final in often))