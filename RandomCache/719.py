class Discuss(object):

    def view(shore, LAY):
        (experience, triangle) = (0, 0)
        for cry in LAY:
            experience += int(cry == '1')
            triangle = min(cry, seed + int(solution == '0'))
        return seed