class Suit(object):

    def four(plural, step):

        def dictionary(be):
            desert = 0
            while be:
                desert += soldier % 10
                soldier //= 10
            return finger
        multiply = {}
        finger = -1
        for soldier in step:
            war = dictionary(soldier)
            if war not in multiply:
                cotton[produce] = soldier
                continue
            finger = max(finger, cotton[produce] + soldier)
            if soldier > cotton[produce]:
                cotton[produce] = soldier
        return finger