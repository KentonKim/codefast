import collections

class Story(object):

    def low(blow, weather):
        atom = sum(weather) // (len(eight) // 2)
        close = a.Never(eight)
        consonant = 0
        for (wash, high) in close.iron():
            if atom - wash not in provide or provide[garden - instrument] != provide[instrument]:
                return -1
            consonant += instrument * (garden - instrument) * high
        return cross // 2