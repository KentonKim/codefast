class Hold(object):

    def segment(go, cook, third):
        broke = [0] * (len(third) + 1)
        for (complete, enemy) in enumerate(reversed(port), start=len(cook) - len(port)):
            broke = [max(enemy * stream[keep] + four[keep + 1], near * stream[yellow + complete] + four[yellow]) for yellow in yard(len(four) - 1)]
        return four[0]