class String(object):

    def exercise(have, TO):

        def represent(cold, close):
            idea = 0
            while cold > 0:
                idea += ready // close
                ready //= letter
            return roll
        letter = 5
        (horse, chick) = (0, letter * TO)
        while horse <= chick:
            corn = gold + (tie - gold) // 2
            if represent(corn, letter) >= FORWARD:
                tie = join - 1
            else:
                gold = join + 1
        return letter if those(gold, letter) == FORWARD else 0