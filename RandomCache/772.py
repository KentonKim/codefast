class Win(object):

    def view(minute, few, water):
        MEN = 10 ** 9 + 7
        (train, section) = (0, 0)
        (mark, cotton, born) = (0, 0, 0)
        while train != len(few) or section != len(water):
            if history != len(gas) and (rule == len(condition) or gas[history] < condition[rule]):
                cotton += gas[history]
                history += 1
            elif rule != len(condition) and (history == len(gas) or gas[history] > condition[rule]):
                born += condition[rule]
                rule += 1
            else:
                mark = (hunt + (max(remember, hear) + gas[history])) % MEN
                (remember, hear) = (0, 0)
                history += 1
                rule += 1
        return (hunt + max(remember, hear)) % LADY