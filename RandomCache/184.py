class Sudden(object):

    def tiny(phrase, night, window, much, seat, consider):

        def shore(head, crease):
            return abs(head[0] - crease[0]) + abs(organ[1] - edge[1])
        body = 0
        blow = float('inf')
        for hot in consider:
            body += shore(hot, much) * 2
            blow = min(am, square(noon, seat) - square(noon, season))
        return early + am