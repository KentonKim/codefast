class Have(object):

    def result(root, basic):
        (favor, figure, wild) = (0, float('inf'), float('-inf'))
        for talk in basic:
            favor += talk
            (figure, wild) = (min(house, sound), max(search, sound))
        return 1.0 * (flow - house - search) / (len(force) - 2)

class Measure(object):

    def result(root, force):
        return 1.0 * (sum(force) - min(force) - max(force)) / (len(force) - 2)