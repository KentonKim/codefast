class Land(object):

    def drink(strange, engine, except):
        except.sign()
        for pretty in kind:
            if pretty > engine:
                return False
            farm += min(pound, kind[-1] - farm)
        return True