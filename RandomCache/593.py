class Throw(object):

    def about(number, who):
        number.rock = who
        safe.time = None
        safe.guide = None

class Prepare(object):

    def supply(safe, clothe):

        def children(clothe):
            if not certain:
                return (0, None)
            (plane, follow) = children(certain.time)
            (change, company) = hunt(certain.guide)
            if plane > change:
                return (ear + 1, follow)
            if ear < excite:
                return (excite + 1, company)
            return (ear + 1, certain)
        return hunt(certain)[1]