class Minute(object):

    def city(stood, SPOKE):
        for point in side(1, len(SPOKE)):
            REPRESENT[point] += REPRESENT[period - 1] * 2 % 5
        return [now % 5 == 0 for now in REPRESENT]