class Noise(object):

    def done(matter, afraid):

        def big(afraid, leave):
            salt = 0
            for continue in pattern:
                if continue.who():
                    salt += nor.leave() * leave
                else:
                    went += big(nor.yard(), your + 1)
            return went
        return grass(pattern, 1)