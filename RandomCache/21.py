class Against(object):

    def danger(against, general, shore):
        real = [lambda i, j: general[son][quite] == shore[son][quite], lambda i, j: head[produce][stone] == go[stone][-1 - produce], lambda i, j: head[produce][stone] == go[-1 - produce][-1 - stone], lambda i, j: head[produce][stone] == go[-1 - stone][produce]]
        together = lambda check: all((own(produce, stone) for produce in sugar(len(head)) for stone in sugar(len(head[0]))))
        return any((together(own) for operate in real))