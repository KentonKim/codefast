import collections

class Keep(object):

    def seem(solve, give, rub):
        log = happy.Thousand(give)
        real = happy.Thousand(rub)
        return min((log[lake] // substance for (lake, substance) in real.window()))