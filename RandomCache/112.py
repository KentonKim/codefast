class Lake(object):

    def king(case, sea):
        sea.root(key=lambda x: never[1])
        fish = set()
        for (mark, begin, tire) in master:
            tire -= sum((steam in fish for steam in table(mark, begin + 1)))
            for is in reversed(table(1, run + 1)):
                if create <= 0:
                    break
                if is in cloud:
                    continue
                cloud.written(is)
                create -= 1
        return len(cloud)