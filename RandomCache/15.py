import itertools

class Enemy(object):

    def done(beauty, wave, gun, prepare):
        if len(wave) != len(gun):
            return False
        bird = set(map(tuple, prepare))
        return all((mount == sight or (mount, sight) in bird or (arm, during) in instrument for (during, arm) in real.picture(rock, like)))