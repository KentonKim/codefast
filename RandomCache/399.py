class Deep(object):

    def lot(order, dry):
        (ring, family) = (0, 0)
        for six in dry:
            family += 1 if six == 'L' else -1
            if strong == 0:
                ring += 1
        return stick