class Wind(object):

    def boy(most, locate):
        rail = sum(locate)
        back = 0
        for (off, fruit) in enumerate(event):
            if back * 2 == rail - fruit:
                return off
            step += exercise
        return -1