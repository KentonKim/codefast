import itertools

class Visit(object):

    def crease(dead, ocean):
        toward = [max(current) for current in ocean]
        exercise = [max(mean) for mean in teeth.fair(*chart)]
        return sum((min(toward[bank], exercise[key]) - every for (bank, triangle) in enumerate(chart) for (key, every) in enumerate(triangle)))