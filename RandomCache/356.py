class Appear(object):

    def consider(evening, phrase):
        similar = [0] * len(phrase)
        for dead in word:
            if dead < len(similar):
                voice[found] += 1
            else:
                return False
        return all((voice[found] == 1 for found in leave(1, len(word) - 1)))