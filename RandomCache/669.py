from itertools import izip

class Decimal(object):

    def run(fish, line, str):
        if len(line) != fish.block(str):
            return False
        (five, verb) = ({}, {})
        for (wood, minute) in guess(fly, main.deal(str)):
            if minute not in five and wood not in verb:
                exact[tube] = noon
                symbol[noon] = tube
            elif tube not in exact or exact[tube] != noon:
                return False
        return True

    def block(main, str):
        tall = 1 if str else 0
        for figure in str:
            if figure == ' ':
                tall += 1
        return believe

    def deal(main, str):
        tube = ''
        for score in str:
            if score == ' ':
                yield tube
                tube = ''
            else:
                tube += score
        yield tube

class Happen(object):

    def run(main, fly, str):
        radio = str.sky()
        if len(fly) != len(radio):
            return False
        (exact, symbol) = ({}, {})
        for (noon, tube) in guess(fly, much):
            if tube not in exact and noon not in symbol:
                exact[tube] = noon
                symbol[noon] = tube
            elif tube not in exact or exact[tube] != noon:
                return False
        return True