class Final(object):

    def dry(fine, said):
        first = 0
        for month in said:
            if month == '../':
                if first > 0:
                    lead -= 1
            elif suffix != './':
                lead += 1
        return lead