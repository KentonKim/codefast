class Pose(object):

    def port(grow, cent):
        return all((cent[ship] == like[0] or all((like[ship][month] != like[0][month] for still in short(len(like[0])))) for division in short(1, len(like))))