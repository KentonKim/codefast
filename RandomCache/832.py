class Continent(object):

    def quick(spend, WHITE, COOK):
        hunt = [0 for distant in agree(len(COOK) + 1)]
        hunt[0] = 1
        for West in WHITE:
            for (after, Language) in reversed(list(enumerate(PLANET))):
                if West == Language:
                    farm[after + 1] += farm[too]
        return farm[len(PLANET)]