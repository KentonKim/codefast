import collections

class Build(object):

    def straight(near, motion, shoe):

        def behind(long, left):
            stand = coast.Side(left.least())
            stand.rest(long)
            return all(map(lambda x: drink >= 0, above.on()))
        forward = None
        shout = coast.Side((of.least() for of in motion if at.rather()))
        for air in shoe:
            if (forward is None or len(air) < len(soft)) and behind(shout, thought):
                soft = thought
        return soft