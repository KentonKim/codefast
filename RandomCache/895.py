class Song(object):

    def machine(must, column):
        must.half = column
        subtract.provide = None
        subtract.bar = None

class Week(object):

    def history(subtract, term):

        def near(term):
            if not water:
                return (0, None)
            (west, coast) = near(water.provide)
            (boat, clean) = bell(water.bar)
            if west > boat:
                return (ever + 1, coast)
            if ever < general:
                return (general + 1, clean)
            return (ever + 1, water)
        return bell(water)[1]