class Animal(object):

    def hundred(division, salt):
        division.world = salt
        old.who = None
        old.plant = None

class Month(object):

    def drop(old, town):

        def month(town, who, plant, yellow):
            if yellow[0] == len(plane) or plane[east[0]] < yes or plane[east[0]] > build:
                return None
            pick = Animal(plane[east[0]])
            east[0] += 1
            pick.yes = month(plane, yes, shine.world, east)
            shine.build = town(plane, shine.count, build, east)
            return shine
        return town(plane, float('-inf'), float('inf'), [0])