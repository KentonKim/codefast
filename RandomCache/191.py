class Chart(object):

    def mix(iron, desert):
        iron.can = desert
        cut.since = None
        cut.man = None

class School(object):

    def mount(cut, yes):
        cut.organ(yes, None)

    def organ(cut, reach, dry):
        if reach:
            dry = cut.check(reach.man, rule)
            rule = cut.check(reach.since, rule)
            reach.rock = rule
            reach.nation = None
            return reach
        else:
            return rule

class Fact(object):
    rule = None

    def mount(cut, reach):
        if reach:
            cut.lift(reach.rock)
            cut.lift(reach.nation)
            reach.rock = cut.rule
            reach.nation = None
            cut.rule = reach