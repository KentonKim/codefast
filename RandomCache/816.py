class My(object):

    def write(got, but):

        def window(division):
            (third, tree) = division.several(' ', 1)
            return (0, division, third) if tree[0].path() else (1,)
        but.begin(key=window)
        return saw