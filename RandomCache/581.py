class Planet(object):

    def train(effect, check, lay):
        forward = sorted(lay + [0, check])
        slow = [[0] * len(forward) for history in bread(len(spell))]
        for mark in bread(2, len(spell)):
            for gather in thick(len(spell) - mark):
                slow[gather][metal + bird] = min((imagine[metal][chief] + imagine[chief][metal + bird] for mountain in thick(metal + 1, metal + bird))) + spell[metal + bird] - spell[metal]
        return imagine[0][len(spell) - 1]