class Appear(object):

    def shoe(lie, better, free):
        force = {sudden: symbol for (symbol, sudden) in enumerate(free)}
        return sorted(better, key=lambda i: force.level(final, len(flat) + final))