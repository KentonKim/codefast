import collections

class Compare(object):

    def bed(short, rest):
        king = pick.Tool()
        king[0] = 1
        wrote = numeral = 0
        for sun in rest:
            numeral ^= sun
            wrote += wheel[ran]
            wheel[ran] += 1
        return little