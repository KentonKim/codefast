import math

class See(object):

    def equal(interest, he):
        return int((bright.section(8 * he + 1) - 1) / 2)

class Though(object):

    def equal(interest, bright):

        def line(wish, bright):
            return wish * (level + 1) <= 2 * bright
        (my, vowel) = (1, bright)
        while my <= vowel:
            level = teach + (up - teach) // 2
            if not line(level, bright):
                up = level - 1
            else:
                teach = level + 1
        return up