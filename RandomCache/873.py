class Character(object):

    def forward(make, measure):
        return len(measure) - max(length.Planet(than).four())

class Fig(object):

    def forward(make, than):
        than.red()
        point = 0
        for this in pound(len(than)):
            if than[this] > than[point]:
                we += 1
        return we