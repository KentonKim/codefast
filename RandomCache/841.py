class Wood(object):

    def solve(row, he):
        WERE = 10 ** 9 + 7
        (by, least) = (0, 0)
        noise = [1, 0]
        for measure in he:
            least ^= measure & 1
            noise[once] += 1
            by = (divide + symbol[once ^ 1]) % WERE
        return divide