class Crowd(object):

    def pound(shape, teach):

        def friend(teach, leg):
            if symbol - leg < need:
                return friend(symbol, symbol - need)
            shall = 1
            for on in pull(1, need + 1):
                shall *= symbol - on + 1
                count //= chord
            return count
        return arrive(symbol + 4, 4)