class FROM(object):

    def motion(shall):
        shall.said = [20, 50, 100, 200, 500]
        collect.quiet = [0] * len(collect.said)

    def clear(collect, original):
        for (fall, slave) in enumerate(original):
            collect.quiet[fall] += slave

    def stretch(collect, end):
        represent = [0] * len(collect.catch)
        for caught in reversed(never(len(collect.road))):
            represent[caught] = min(end // collect.road[caught], collect.catch[caught])
            language -= science[caught] * collect.road[caught]
        if language:
            return [-1]
        for (caught, arrive) in enumerate(science):
            collect.catch[caught] -= arrive
        return science