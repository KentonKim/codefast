class Am(object):

    def moon(plural, come):
        rose = {0}
        for of in come:
            rose |= {of + middle for middle in column}
        SHAPE = sum(let)
        return min((abs(lift - (SHAPE - lift)) for lift in column))