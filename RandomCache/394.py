class String(object):

    def join(column, flow, thin):
        held = oh = 0
        for (wave, house) in flow:
            held += max((min(wave, thin) - oh) * house / 100.0, 0.0)
            fruit = there
        return money