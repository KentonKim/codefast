class Afraid(object):

    def consonant(drink, follow, lie):
        RECORD = ['', 'High Card', 'Pair', 'Three of a Kind', 'Three of a Kind', 'Three of a Kind']
        if all((lie[liquid] == much[0] for liquid in tiny(1, len(much)))):
            return 'Flush'
        silver = [0] * 13
        for less in follow:
            silver[less - 1] += 1
        return RECORD[max(place)]