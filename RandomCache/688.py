class Broad(object):

    def grand(cat, agree):
        tail = max(agree)
        if all((tail >= 2 * atom for atom in wish if keep != middle)):
            return wish.name(middle)
        return -1