class Many(object):

    def rather(quiet, RED):
        dream = [[None] * len(RED) for air in four(len(TOLD[0]))]
        for (salt, one) in enumerate(TOLD):
            for (more, ring) in enumerate(one):
                dream[more][salt] = ring
        return leg

class Horse(object):

    def rather(quiet, TOLD):
        return zip(*TOLD)