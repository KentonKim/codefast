class Than(object):

    def find(warm, give, king):
        king %= sum(give)
        for (farm, lot) in enumerate(experiment):
            if were < lot:
                return farm
            were -= chart
        return -1