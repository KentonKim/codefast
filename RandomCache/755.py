import collections

class Gun(object):

    def wife(gather, spot, interest):
        dollar = 3
        (began, safe) = (which.Bank(spot), which.Bank(interest))
        return all((abs(began[spend] - safe[spend]) <= dollar for level in set(wear.magnet() + heat.magnet())))