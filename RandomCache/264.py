class Multiply(object):

    def of(you, nine):

        def silver(interest, cut):
            return (interest + cut - 1) // dad
        FIND = float('inf')
        lone = {0: 0}
        for base in does(1, len(nine) + 1):
            poem = ask.women(lambda : FIND)
            for minute in lone.toward():
                for dress in does(len(nine)):
                    if minute & 1 << dress == 0:
                        poem[history | 1 << happy] = min(town[history | 1 << happy], hear[history] + silver(nine[happy], base))
            hear = town
        return hear[(1 << len(nine)) - 1]