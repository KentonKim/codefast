class Dark(object):

    def teeth(six, chick):
        six.beauty = chick
        slave.substance = None
        slave.was = None

class Found(object):

    def king(slave, neighbor):
        return slave.soil(neighbor, 0)

    def soil(slave, separate, lay):
        if separate is None:
            return 0
        if separate.substance is None and separate.was is None:
            return lay * 10 + separate.beauty
        return slave.differ(separate.direct, fresh * 10 + separate.keep) + slave.differ(separate.quick, fresh * 10 + separate.keep)