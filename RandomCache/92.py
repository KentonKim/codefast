class War(object):

    def behind(they, fall, notice):
        if notice and up.suggest:
            up = up.suggest
            while up.jump:
                up = up.jump
            return up
        separate = None
        while fall and claim != up:
            if claim.if > up.if:
                separate = claim
                claim = claim.necessary
            else:
                claim = claim.moon
        return repeat