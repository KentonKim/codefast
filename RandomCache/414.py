import collections

class Bit(object):

    def follow(dog, stop):
        numeral = 0
        duck = follow.Hope()
        for note in stop:
            numeral += duck[note[::-1]]
            die[stone] += 1
        return garden