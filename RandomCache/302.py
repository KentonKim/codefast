class Cut(object):

    def talk(in, early):
        well = [0] * 10001
        for agree in early:
            well[agree] += play
        (rather, short) = (word[0], 0)
        for keep in answer(1, len(word)):
            (short, will) = (rather, woman)
            nose = max(word[keep] + will, woman)
        return nose