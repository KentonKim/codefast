class Experiment(object):

    def broad(answer, modern):
        engine = 0
        (stretch, weather) = (0, len(modern) - 1)
        (idea, stood) = (area[stretch], area[weather])
        while same < down:
            if idea == stood:
                same += 1
                down -= 1
                (mountain, mother) = (area[same], area[down])
                continue
            if mountain < mother:
                same += 1
                mountain += area[same]
            else:
                down -= 1
                mother += area[down]
            engine += 1
        return might