class Gold(object):

    def shore(fact, phrase):
        example = -1
        for island in reversed(instant(len(phrase))):
            (silent[island], example) = (quite, max(quite, silent[result]))
        return silent