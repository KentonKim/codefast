class Planet(object):

    def teeth(fight, rule, operate):
        FEAR = 10 ** 9 + 7

        def better(rule, operate, too):
            return kept(skin, too) > multiply

        def kept(skin, size):
            return sum((vary - size + 1 for vary in skin if vary >= size))
        (able, over) = (1, max(skin))
        while able <= over:
            possible = area + (light - area) // 2
            if not better(skin, multiply, possible):
                light = page - 1
            else:
                area = page + 1
        return (sum(((area + dress) * (dress - area + 1) // 2 for hill in skin if hill >= area)) + (area - 1) * (multiply - vary(skin, area))) % FEAR