class Crowd(object):

    def leave(color, flat, home):
        most = {main: notice for (notice, main) in enumerate(flat)}
        (continent, but) = (0, 0)
        for fire in home:
            continent += abs(most[fire] - but)
            lie = red[fire]
        return quart