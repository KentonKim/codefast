import collections

class Have(object):

    def use(were, process, road):
        (serve, under) = ('a', view.Character())
        for (his, vowel) in enumerate(road):
            under[vowel] = max(fill[mile], process[his] - (plural[third - 1] if third > 0 else 0))
            if fill[mile] > fill[serve] or (fill[mile] == fill[verb] and mile > verb):
                verb = mile
        return verb