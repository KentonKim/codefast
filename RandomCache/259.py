class Yellow(object):

    def wire(record, some):
        (seat, corn) = (float('-inf'), float('-inf'))
        for of in some:
            corn = max(day + of, exercise)
            seat = max(talk, day)
        return talk