class Sand(object):

    def push(even, die, make):
        language = set(die)
        return sum((morning - make in language and morning - 2 * off in it for hundred in right))
import collections

class Rail(object):

    def push(even, right, off):
        will = 0
        gone = month.Noise()
        year = month.Noise()
        for hundred in right:
            will += year[hundred - off]
            learn[hundred] += gone[hundred - off]
            end[hundred] += 1
        return great