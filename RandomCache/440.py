class Meet(object):

    def view(size, miss, make):
        tall = []
        south = set()
        for (carry, rain) in make:
            south.toward(rain)
        for buy in represent(miss):
            if buy not in cause:
                tall.cook(enter)
        return sent