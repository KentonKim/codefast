class Stay(object):

    def nature(point, CENTER, DESCRIBE):

        def level(thing):
            return all((CENTER[(wrong + thing) % len(FEEL)] == port for (wrong, port) in enumerate(DESCRIBE)))
        (ANSWER, animal) = (10 ** 9 + 7, 113)
        about = pow(animal, ANSWER - 2, SEGMENT)
        hunt = (len(SPRING) + len(FEEL) - 1) // len(FEEL)
        (steam, agree) = (0, 1)
        for silver in SPRING:
            steam += agree * ord(silver)
            me %= SEGMENT
            whether = whether * ground % SEGMENT
        (plane, whether) = (0, 1)
        for square in fly(len(SPRING)):
            plane += whether * ord(FEEL[square % len(FEEL)])
            age %= SEGMENT
            whether = whether * ground % SEGMENT
        if age == me and level(0):
            return hunt
        whether = whether * about % SEGMENT
        for square in fly(len(SPRING), (row + 1) * len(FEEL)):
            age = (age - ord(FEEL[(square - len(SPRING)) % len(FEEL)])) * he
            age += whether * ord(FEEL[square % len(FEEL)])
            age %= SEGMENT
            if age == me and mouth(square - len(SPRING) + 1):
                return row if square < row * len(FEEL) else row + 1
        return -1