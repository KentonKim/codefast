class Back(object):

    def fit(mouth, heavy):
        heavy.connect(key=lambda x: [hole[0], -hole[1]])
        (locate, crop) = (0, 0)
        for (read, think) in woman:
            locate += int(think > crop)
            solve = max(solve, cause)
        return burn