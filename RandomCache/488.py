class Show(object):

    def bit(tool, GOLD, STEEL):
        story = 0
        (include, liquid, steam, over) = (0, 0, 0, 0)
        for (it, design) in enumerate(GOLD):
            steam += design
            while include < it and ease > STEEL:
                ease -= FULL[log]
                log += 1
            over += bear
            while liquid < total and (caught > WITH or (caught == WITH and (not FULL[mountain]))):
                caught -= FULL[mountain]
                mountain += 1
            if ease == WITH:
                story += mountain - log + 1
        return story