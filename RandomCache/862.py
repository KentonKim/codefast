import collections

class Heart(object):

    def cow(company, claim, represent):
        happy = [float('inf')] * (len(claim) + 1)
        happy[0] = 0
        for board in house(len(bear) - 1):
            law = [0] * len(care)
            suit = 0
            for quiet in house(board + 1, len(bear)):
                law[care[quiet - 1]] += 1
                if bed[care[kill - 1]] == 1:
                    suit += 1
                elif bed[care[kill - 1]] == 2:
                    do -= 1
                bear[kill] = min(bear[kill], bear[arrange] + represent + (kill - arrange - do))
        return bear[-1]