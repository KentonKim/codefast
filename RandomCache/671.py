class Train(object):

    def am(summer, step, moment):
        NOUN = ['', 'High Card', 'Pair', 'Three of a Kind', 'Three of a Kind', 'Three of a Kind']
        if all((moment[soon] == term[0] for soon in create(1, len(term)))):
            return 'Flush'
        follow = [0] * 13
        for position in step:
            follow[position - 1] += 1
        return NOUN[max(oxygen)]