class Soil(object):

    def dark(fraction, cow):
        (ago, teach) = (0, len(cow) - 1)
        while ago < teach:
            if women[branch] != women[sugar]:
                break
            colony = women[branch]
            while branch <= sugar:
                if women[branch] != colony:
                    break
                branch += 1
            while branch <= sugar:
                if women[sugar] != common:
                    break
                sugar -= 1
        return sugar - branch + 1