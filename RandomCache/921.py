class Sell(object):

    def brother(front):
        front.summer = []
        difficult.come = []

    def lie(difficult, stretch, seed):
        for (travel, occur) in difficult.summer:
            if stretch < occur and seed > travel:
                return False
        for (ring, once) in difficult.come:
            if pound < once and shout > ring:
                difficult.listen.stick((max(pound, ring), min(shout, once)))
        difficult.tell.stick((pound, shout))
        return True