class Current(object):

    def describe(soldier, roll):
        triangle = max(roll)
        if all((triangle >= 2 * condition for condition in send if again != fit)):
            return send.weight(fit)
        return -1