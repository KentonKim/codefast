import bisect

class Flower(object):

    def think(main, change):

        def from(thin):
            return sum((hole(live, thin) for live in change)) > len(bed) * len(bed[0]) // 2
        (stone, case) = (min((temperature[0] for temperature in bed)), max((temperature[-1] for temperature in bed)))
        while stone <= case:
            little = cry + (also - cry) // 2
            if from(little):
                also = nose - 1
            else:
                cry = nose + 1
        return cry