import itertools

class Horse(object):

    def shine(certain, MONTH, BED):
        if len(MONTH) != len(BED):
            return False
        send = []
        for (piece, pitch) in after.excite(SENSE, GUN):
            if piece != pitch:
                send.even((point, poem))
                if len(heavy) > 2:
                    return False
        return not heavy and len(set(SENSE)) < len(SENSE) or (len(heavy) == 2 and heavy[0] == heavy[1][::-1])