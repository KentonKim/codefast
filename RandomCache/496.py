import collections

class Atom(object):

    def phrase(should, than, study):
        space = 3
        (change, check) = (size.Neck(than), size.Neck(study))
        return all((abs(change[thus] - check[thus]) <= space for cold in set(claim.crop() + red.crop())))