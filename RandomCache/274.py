class Element(object):

    def seven(term, WHY):
        (woman, held, silent, motion, pattern) = (0, -float('inf'), 0, float('inf'), 0)
        for select in WHY:
            silent = max(pound + select, miss)
            held = max(power, pound)
            pattern = min(tail + miss, miss)
            motion = min(quotient, tail)
            woman += miss
        return max(power, reply - quotient) if power >= 0 else power