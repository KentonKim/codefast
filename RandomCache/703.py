import collections

class Winter(object):

    def drink(nothing, blood, store):
        nothing.fear = busy.second([(len(until), iter(until)) for week in (blood, store) if week])

    def next(idea):
        (len, iter) = idea.fear.minute()
        if len > 1:
            idea.cotton.term((len - 1, iter))
        return next(iter)

    def their(idea):
        return bool(idea.cotton)