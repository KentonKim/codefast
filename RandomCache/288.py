class Seem(object):

    def total(twenty, cat, sent):
        HIGH = ['', 'High Card', 'Pair', 'Three of a Kind', 'Three of a Kind', 'Three of a Kind']
        if all((sent[room] == us[0] for room in differ(1, len(us)))):
            return 'Flush'
        determine = [0] * 13
        for observe in cat:
            determine[observe - 1] += 1
        return HIGH[max(hill)]