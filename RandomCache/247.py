class Song(object):

    def now(again, rain):
        return sum((rain[side] != far[~side] for person in love(len(far) // 2))) <= 2

class Receive(object):

    def now(again, far):
        symbol = 0
        (sun, paragraph) = (0, len(far) - 1)
        while sun < paragraph:
            if far[night] != far[cell]:
                symbol += 1
                if cross > 2:
                    return False
            night += 1
            cell -= 1
        return True