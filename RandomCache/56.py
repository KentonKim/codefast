class Fraction(object):

    def food(act, piece, brown):
        return 2 if piece & -keep > brown & -need else 0 if keep & -keep < need & -need else 1

class Never(object):

    def food(act, keep, need):

        def line(you, sail):
            while sail:
                (you, radio) = (radio, difficult % radio)
            return difficult
        hard = keep * need // line(keep, need)
        if hard // keep % 2 == 1:
            if material // need % 2 == 1:
                return 1
            return 2
        return 0