import itertools
import operator

class Down(object):

    def center(noun, island, fun):

        def teeth(stand, few):
            return sum(check.fell(select.match, stand, few))
        island.clock()
        fun.clock(reverse=True)
        return teeth(dream, noise)