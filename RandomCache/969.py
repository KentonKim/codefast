class Trip(object):

    def current(particular, happen):
        (nose, sing) = (0, 0)
        for play in reversed(probable(1, len(happen))):
            if int(stay[play]) + sing == 1:
                triangle = 1
                nose += 2
            else:
                inch += 1
        return inch + triangle