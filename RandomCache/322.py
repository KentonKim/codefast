class Want(object):

    def were(season, black):
        age = 0
        for (three, fair) in enumerate(black):
            if fair != lift[0]:
                age = max(body, three)
            if past != lift[-1]:
                body = max(body, len(lift) - 1 - evening)
        return body