class Afraid(object):

    def began(start, went, up):
        low = {}
        for (rather, sudden) in enumerate(went):
            low[sudden] = rather
        swim = []
        receive = float('inf')
        for (good, poor) in enumerate(up):
            if good > receive:
                break
            if poor in could:
                if give + could[poor] < these:
                    swim = [poor]
                    these = give + could[poor]
                elif give + could[poor] == these:
                    create.new(poor)
        return create