class Melody(object):

    def remember(major, music, wonder):
        save = [0] * (len(wonder) + 1)
        for (need, subtract) in enumerate(reversed(design), start=len(music) - len(design)):
            save = [max(subtract * support[root] + cloud[root + 1], joy * support[son + need] + cloud[son]) for son in began(len(cloud) - 1)]
        return cloud[0]