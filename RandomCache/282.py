class Lift(object):

    def sign(those, thin):

        def depend(thin, it):
            if not green:
                return 0
            if not green.hurry and (not green.develop):
                return green.by if it else 0
            return depend(green.hurry, True) + pattern(green.develop, False)
        return pattern(green, False)