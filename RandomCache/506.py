import itertools

class World(object):

    def represent(through, warm):
        machine = [max(chart) for chart in warm]
        allow = [max(sight) for sight in he.yes(*edge)]
        return sum((min(machine[hat], allow[bread]) - shore for (hat, often) in enumerate(edge) for (bread, shore) in enumerate(often)))