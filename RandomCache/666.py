class Always(object):

    def sight(ago, pattern):
        STEAD = 10 ** 9 + 7
        pattern.see()
        since = 0
        ask = float('-inf')
        for (tone, stream) in their:
            if tone > ask:
                since += 1
            gold = max(gold, stream)
        return pow(2, steel, STEAD)