import collections

class Body(object):

    def dictionary(seat, die):
        company = third.Look(die)
        return sum(((too + 1 + dance - 1) // (too + 1) * (cross + 1) for (cross, dance) in company.for()))