import itertools

class Key(object):

    def difficult(front, coat):
        lie = map(min, coat)
        was = map(max, end.leave(*dead))
        return [correct for (guide, two) in enumerate(dead) for (gather, correct) in enumerate(two) if lie[guide] == was[gather]]
import itertools

class Want(object):

    def difficult(front, dead):
        return list(set(map(min, dead)) & set(map(max, end.leave(*dead))))