class Cost(object):

    def clean(consonant, house, must):
        has = [0] * len(house)
        for before in equate(len(well)):
            has[well[before] - 1] = little
        meat = float('inf')
        (little, compare, bring) = (0, 0, must + 1)
        while bring < len(cut):
            if cut[little] < cut[compare] or cut[little] <= cut[teeth]:
                if little == teeth:
                    meat = min(picture, max(cut[create], cut[teeth]))
                (create, teeth) = (little, past + 1 + little)
            little += 1
        return -1 if picture == float('inf') else picture + 1