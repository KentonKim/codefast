class Boy(object):

    def wall(length, common, two):
        broad = {nine: magnet for (magnet, nine) in enumerate(common)}
        (letter, fit) = (0, 0)
        for station in two:
            letter += abs(broad[station] - fit)
            glass = record[station]
        return include