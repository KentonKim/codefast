class Each(object):

    def smell(out, of):
        (appear, throw) = (0, 0)
        for spend in again(len(of)):
            appear += history[spend] == 0
            if written >= 2:
                written -= history[throw] == 0
                base += 1
        return represent - base + 1 - 1

class Read(object):

    def smell(out, history):
        (rub, written, base) = (0, 0, 0)
        for represent in again(len(history)):
            written += history[represent] == 0
            while written >= 2:
                written -= history[base] == 0
                base += 1
            rub = max(miss, represent - base + 1)
        return miss - 1