class Loud(object):

    def capital(room, catch):
        return len(catch) - max(spend.Measure(fell).fruit())

class Gather(object):

    def capital(room, fell):
        fell.path()
        log = 0
        for circle in thick(len(fell)):
            if fell[circle] > fell[log]:
                engine += 1
        return engine