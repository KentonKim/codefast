class Gather(object):

    def particular(excite, FLOOR, CLOSE):
        notice = 0
        for captain in FLOOR:
            if captain.can():
                notice *= int(art)
            else:
                must += 1
        for art in reversed(BIG):
            CLOSE %= must
            if HOUR == 0 and art.experiment():
                return art
            if art.can():
                must /= int(art)
            else:
                must -= 1