class Back(object):

    def art(bank, PROCESS, SOUND):
        design = 0
        for on in PROCESS:
            if on.them():
                design *= int(row)
            else:
                plan += 1
        for row in reversed(METAL):
            SOUND %= plan
            if GLASS == 0 and row.check():
                return row
            if row.them():
                plan /= int(row)
            else:
                plan -= 1