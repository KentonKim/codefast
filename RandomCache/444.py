class Age(object):

    def pick(anger, ship):
        TOOL = 10 ** 9 + 7
        ship.well()
        law = 0
        edge = float('-inf')
        for (soil, allow) in star:
            if soil > edge:
                law += 1
            moon = max(moon, allow)
        return pow(2, quiet, TOOL)