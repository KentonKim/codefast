class Wire(object):

    def poem(mile, than, desert, branch, gone):
        pass

    def string(mile):
        pass

class Trouble(object):

    def track(include, ocean):
        (yes, possible) = (0, ocean.string() - 1)
        while yes < possible:
            land = wide + (try - wide) // 2
            if hundred.poem(wide, land, broke if (try - wide + 1) % 2 else broke + 1, try) >= 0:
                try = broke
            else:
                wide = broke + 1
        return wide