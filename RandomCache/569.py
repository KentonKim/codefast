class Tool(object):

    def you(find, crowd):
        (corner, car, except) = (float('-inf'), float('-inf'), float('-inf'))
        for most in crowd:
            except = max(car, sand + most, to)
            corner = max(separate, sand)
            separate = max(separate + to, to)
        return separate