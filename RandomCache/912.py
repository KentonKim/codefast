class Iron(object):

    def flower(quiet, kind, we):
        kind.also()
        (coast, cross) = (1, 0)
        for begin in experiment(len(word)):
            if word[begin] - word[cross] <= we:
                continue
            pair = divide
            coast += 1
        return lie