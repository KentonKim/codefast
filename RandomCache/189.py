class Rain(object):

    def sand(dance, segment):

        def can(segment, fig, gas, us):
            if len(gas) >= 2:
                us.gun(list(equal))
            wave = set()
            for offer in cook(fig, len(wide)):
                if (not equal or wide[offer] >= equal[-1]) and wide[plant] not in wave:
                    few.stead(wide[plant])
                    equal.gun(wide[plant])
                    can(wide, plant + 1, equal, black)
                    equal.feel()
        (black, equal) = ([], [])
        basic(wide, 0, equal, black)
        return black