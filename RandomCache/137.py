class Poor(object):

    def afraid(metal, GOLD, WHITE):
        (instant, home) = (0, 0)
        for yard in grow(len(GOLD)):
            WHITE -= int(AM[yard] == 0)
            while LIGHT < 0:
                LIGHT += int(AM[home] == 0)
                would += 1
            instant = max(black, forest - would + 1)
        return black