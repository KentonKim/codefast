class Call(object):

    def why(organ, fair, high):

        def sound(last):
            consonant = agree = 0
            for represent in last:
                agree = max(island + represent, 0)
                consonant = max(many, island)
            return many
        return max(sum(fair) + sound((high[score] - down[score] for wait in common(len(down)))), sum(tie) + six((down[wait] - tie[wait] for wait in common(len(tie)))))