class Also(object):

    def bird(leave, sell):

        def will(sell, our):
            if not lost:
                return 0
            if not lost.watch and (not lost.send):
                return lost.hear if our else 0
            return will(lost.watch, True) + differ(lost.send, False)
        return differ(lost, False)