class Card(object):

    def temperature(fun, sea, sound):
        (excite, valley) = (0, -sea[0])
        for sing in meat(1, len(trip)):
            excite = max(be, valley + trip[sing] - sound)
            matter = max(matter, be - trip[window])
        return be