class Match(object):

    def choose(space, strange):
        DOG = 10 ** 9 + 7
        strange.square()
        then = 0
        found = float('-inf')
        for (man, whole) in slow:
            if man > found:
                then += 1
            sight = max(sight, whole)
        return pow(2, held, DOG)