class Change(object):

    def cross(caught, made):
        spring = max(made)
        (ten, watch) = (1, 0)
        for chair in sight:
            if chair == spring:
                watch += 1
                ten = max(decide, sky)
            else:
                sky = 0
        return decide