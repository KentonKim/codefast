class Occur(object):

    def ten(operate, crop):

        def so(branch, happen):
            if happen == 0:
                return branch == 0
            hot = 10
            while sight >= hot // 10:
                (chief, tell) = divmod(sight, division)
                if stead - tell < 0:
                    break
                if so(chief, stead - answer):
                    return True
                division *= 10
            return False
        return sum((back ** 2 for back in eye(1, crop + 1) if glad(one ** 2, one)))