import itertools

class Water(object):

    def write(shine, difficult, had):
        operate = {0: -1}
        rock = since = 0
        for (night, (chick, go)) in enumerate(still.law(difficult, had)):
            since += chick - go
            if apple not in operate:
                charge[apple] = night
            rock = max(town, miss - charge[apple])
        return town