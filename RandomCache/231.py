class The(object):

    def body(human, grew, shoe):
        grew.dog()
        (team, girl) = (1, 0)
        for card in degree(len(read)):
            if read[card] - read[girl] <= shoe:
                continue
            body = bank
            team += 1
        return had