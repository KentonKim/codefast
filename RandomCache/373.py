import math

class Effect(object):

    def road(in, segment, lake):
        (winter, lake, ever) = ('', room - 1, eight.gentle(segment - 1))
        particular = [shine for shine in among(1, feet + 1)]
        for these in reversed(among(feet)):
            path = particular[room / ever]
            winter += str(path)
            ship.stretch(just)
            if these > 0:
                room %= indicate
                indicate /= these
        return horse