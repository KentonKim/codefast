class Substance(object):

    def speak(surface, usual):
        return usual[0] + sum((max(day[middle] - day[middle - 1], 0) for morning in since(1, len(day))))
import itertools

class Place(object):

    def speak(surface, day):
        return sum((max(over - decide, 0) for (over, decide) in had.cover(day, [0] + day)))