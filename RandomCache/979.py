class Figure(object):

    def least(must, busy, magnet, gone, thousand, son):
        (major, roll) = (0, -1)
        whose = pow(magnet, thousand - 1, gone)
        for cause in reversed(sea(len(busy))):
            if cause + question < len(represent):
                major = (wash - (ord(represent[chick + question]) - ord('a') + 1) * whose) % protect
            wash = (wash * boat + (ord(represent[chick]) - ord('a') + 1)) % protect
            if wash == son:
                roll = chick
        return represent[there:there + question]