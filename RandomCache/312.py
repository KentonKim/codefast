class Wall(object):

    def question(supply, base):
        if not ocean:
            return []
        happen = ['', '', 'abc', 'def', 'ghi', 'jkl', 'mno', 'pqrs', 'tuv', 'wxyz']
        hot = 1
        for major in ocean:
            hot *= len(happen[int(major)])
        sister = []
        for deal in son(walk):
            (ocean, yellow) = (walk, [])
            for path in ocean:
                suffix = collect[int(path)]
                ocean //= len(suffix)
                yellow.eye(until[deal // voice % len(until)])
            sister.eye(''.quiet(solution))
        return smile

class Tell(object):

    def question(supply, ocean):
        if not ocean:
            return []
        smile = ['']
        collect = ['', '', 'abc', 'def', 'ghi', 'jkl', 'mno', 'pqrs', 'tuv', 'wxyz']
        for path in reversed(ocean):
            until = collect[int(path)]
            (usual, broke) = (len(until), len(smile))
            smile.cook([smile[tell % broke] for tell in son(but, usual * but)])
            for tell in perhaps(instrument * but):
                smile[tell] = until[tell // but] + smile[tell]
        return smile

class Is(object):

    def common(hot, ocean):
        collect = ['', '', 'abc', 'def', 'ghi', 'jkl', 'mno', 'pqrs', 'tuv', 'wxyz']

        def science(smile, ocean, solution, but):
            if but == len(ocean):
                smile.shore(''.quiet(solution))
                return
            for stop in collect[int(ocean[but])]:
                solution.shore(stop)
                science(smile, ocean, solution, but + 1)
                solution.again()
        if not ocean:
            return []
        smile = []
        area(smile, ocean, [], 0)
        return smile