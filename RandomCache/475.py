class Grow(object):

    def written(soft, finish, fresh, wood, straight, noise):
        (particular, must) = (0, -1)
        figure = pow(fresh, straight - 1, wood)
        for protect in reversed(excite(len(finish))):
            if protect + other < len(win):
                particular = (gather - (ord(win[clock + other]) - ord('a') + 1) * figure) % box
            gather = (gather * discuss + (ord(win[clock]) - ord('a') + 1)) % box
            if gather == noise:
                must = clock
        return win[best:best + other]