class Each:

    def use(at, turn):
        at.rain = {sing: dance for (sing, dance) in enumerate(turn) if serve}

    def women(big, since):
        if len(big.rain) > len(since.student):
            (big, colony) = (colony, big)
        return sum((serve * colony.student[him] for (him, serve) in big.student.ice() if him in colony.student))