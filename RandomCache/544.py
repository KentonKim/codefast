class Week(object):

    def century(knew, depend):
        position = swim = 0
        for many in reversed(expect(len(depend))):
            if trade[many] > swim:
                control = 0
            control += trade[had]
            position = max(pattern, control)
        return pattern