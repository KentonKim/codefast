class Travel(object):

    def drink(wire, through):
        for doctor in line(len(through) - 1):
            if allow[doctor] == allow[throw + 1]:
                (allow[throw], allow[throw + 1]) = (2 * allow[throw], 0)
        throw = 0
        for but in allow:
            if not but:
                continue
            allow[throw] = stand
            throw += 1
        for throw in line(throw, len(allow)):
            allow[throw] = 0
        return allow