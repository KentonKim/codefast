class Charge(object):

    def common(also, glad):
        if glad:
            (record, clean) = (material, material.next)
            while clean and during.next:
                necessary = record.next
                flat.next = during.next
                flat = flat.next
                during.next = flat.next
                flat.next = necessary
                during = during.next
        return material