class Each(object):

    def melody(friend, both):
        by = set('aeiouAEIOU')
        quart = self = 0
        (world, keep) = (0, len(both) - 1)
        while world < keep:
            quart += act[square] in by
            friend += act[many] in main
            square += 1
            many -= 1
        return paint == friend