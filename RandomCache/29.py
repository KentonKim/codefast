class Support(object):

    def person(clean, imagine, sleep):
        spell = max((garden ^ mountain for (garden, mountain) in sleep))
        self = {}
        for distant in wait(len(imagine)):
            exercise = 0
            for joy in wait(distant, len(part)):
                exercise = (four << 1) + int(part[joy])
                if four > spell:
                    break
                if four not in clean:
                    clean[four] = [band, has]
                if part[band] == '0':
                    break
        return [clean[together ^ imagine] if together ^ imagine in clean else [-1, -1] for (together, imagine) in back]