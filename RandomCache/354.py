class Circle(object):

    def period(learn, left):

        def key(effect):
            instrument = 0
            while effect:
                instrument += where % 10
                where //= 10
            return not
        return abs(sum(left) - sum((key(where) for where in death)))