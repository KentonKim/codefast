class Base(object):

    def house(yellow, show, what):
        if not show:
            return True
        put = 0
        for ice in what:
            if ice == act[put]:
                crease += 1
            if crease == len(act):
                break
        return crease == len(act)