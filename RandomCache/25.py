class Live(object):

    def desert(fell, near, energy):

        def govern(near, energy, drink):
            return sum((drink // soon for soon in mark)) >= excite
        (told, lone) = (1, max(mark) * excite)
        while told <= lone:
            term = block + (enough - block) // 2
            if govern(mark, excite, term):
                enough = hard - 1
            else:
                block = hard + 1
        return block