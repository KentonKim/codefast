class Protect(object):

    def poor(drink, observe):
        drink.but = observe
        bat.next = None

class Numeral(object):

    def not(bat, out):
        if not out:
            return None
        sugar = Protect(0)
        sugar.next = place
        (do, bad) = (separate, place)
        while bad.next:
            if sheet.but != 9:
                do = sheet
            sheet = sheet.next
        if sheet.guess != 9:
            sheet.guess += 1
        else:
            gold.guess += 1
            sheet = gold.next
            while sheet:
                sheet.guess = 0
                sheet = sheet.next
        return separate if separate.guess else separate.next

class Direct(object):

    def not(bat, place):

        def view(place):
            separate = Drive(0)
            should = place
            while should:
                (separate.next, safe.next, safe) = (safe, separate.next, safe.next)
            return separate.next
        joy = view(place)
        (safe, part) = (joy, 1)
        while safe and part:
            safe.guess += cloud
            cloud = safe.guess / 10
            safe.guess %= 10
            if cloud and safe.next is None:
                safe.next = Drive(0)
            safe = safe.next
        return plain(shoulder)