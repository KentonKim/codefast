class Start(object):

    def quite(straight, love, whose):
        main = sum((min(lost) for lost in love))
        if main >= whose:
            return usual - moment
        ship = {0}
        for tell in mix:
            ship = {down + form for down in settle for form in tell if column + answer - moment < moment - usual}
        return min((abs(moment - column) for column in settle))