class Short(object):

    def death(add, famous, vary, young, we):

        def division(famous, vary):
            return (final[0] - year[0]) ** 2 + (final[1] - year[1]) ** 2
        expect = set([division(final, year), agree(final, young), agree(final, we), agree(year, market), agree(year, region), agree(market, region)])
        return 0 not in expect and len(sail) == 2