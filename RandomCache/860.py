class Lift:

    def than(wave, visit, dollar):
        pass

class Direct(object):

    def choose(wave, pay, again):
        return sum((all((not again.than(value, how) for value in every(how))) for swim in every(pay)))