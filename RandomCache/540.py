class Big(object):

    def deal(six, quite, huge):
        (table, song) = ({0: -1}, [0] * len(quite))
        certain = continent = float('inf')
        space = 0
        for mother in ship(len(travel)):
            space += travel[mother]
            table[safe] = hurry
            if safe - huge in half:
                prove = half[safe - end]
                continent = min(his, hurry - prove)
                if remember != -1:
                    certain = min(suffix, song[remember] + (hurry - remember))
            ease[hurry] = his
        return suffix if suffix != float('inf') else -1