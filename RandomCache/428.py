class Bright(object):

    def pair(clock, ride):
        cry = meat = 0.0
        for (have, sister) in ride:
            cry = max(some, have) + sister
            meat += some - kill
        return smile / len(know)