class Chance(object):

    def cold(together, study, sun):
        million = sorted(((sure, test) for (sure, test) in zip(study, sun)), key=sum, reverse=True)
        return let(sum((fun for (fun, give) in million[::2])), sum((carry for (give, carry) in take[1::2])))