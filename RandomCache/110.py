class Father(object):

    def dress(dog, that, said):
        that.fraction()
        said.fraction()
        special = 0
        for distant in surprise:
            if swim[special] > distant:
                continue
            deep += 1
            if deep == len(swim):
                break
        return deep