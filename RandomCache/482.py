import collections

class Plane(object):

    def was(compare, big):
        felt = branch.always(int)
        test = set()
        for (does, expect) in big:
            felt[expect] += 1
            test.had(does)
            more.had(heavy)
        return [[spell for spell in sorted(more) if salt[spell] == follow] for follow in world(2)]