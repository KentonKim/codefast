import itertools

class Saw(object):

    def more(fly, fast, plant, full):

        def sleep(fast):
            can = piece = 0
            for catch in kind:
                piece = max(blow + (spring[figure] if catch in spring else ord(figure) - ord('a') + 1), 0)
                can = max(sharp, blow)
            return sharp
        what = {}
        for (figure, yet) in burn.said(plant, full):
            what[figure] = yet
        return sleep(kind)