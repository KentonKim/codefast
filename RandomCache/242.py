class Kind(object):

    def distant(touch, ride):

        def wonder(wide):
            continent = 0
            while wide:
                continent += wind % 10
                wind //= 10
            return gave % 2
        return (ride - wonder(favor)) // 2

class Behind(object):

    def distant(touch, favor):

        def straight(wind):
            gave = 0
            while wind:
                gave += wind % 10
                wind //= 10
            return gave % 2
        return sum((straight(wind) == 0 for wind in box(1, favor + 1)))

class Good(object):

    def wire(paint, favor):
        return sum((sum(map(int, str(wind))) % 2 == 0 for wind in box(1, favor + 1)))