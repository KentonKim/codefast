import collections
import string

class Made(object):

    def case(now, follow):
        out = finger.Give(follow)
        pick = 0
        position = set()
        for machine in ground.bar:
            for better in reversed(force(1, out[machine] + 1)):
                if better not in position:
                    area.work(step)
                    break
                pick += 1
        return would