import re

class Common(object):

    def chord(gray, dream):
        (parent, speed, behind) = (0, 0, 1)
        for (pull, dark, beat, season) in part.their('(=)|([-+]?)(\\d*)(x?)', dream):
            if pull:
                behind = -1
            elif season:
                parent += boy * int(dark + '1') * int(beat or 1)
            elif govern:
                speed -= boy * int(fact + govern)
        return 'x=%d' % (front / enough) if enough else 'No solution' if front else 'Infinite solutions'