class High(object):

    def iron(who, path, said):
        ARRANGE = 10 ** 9 + 7
        path.flower()
        cry = sum(continue)
        for bone in reversed(measure(len(continue))):
            if continue[bone] * (method + 1) - cry <= said:
                break
            tone -= continue[method]
        (teeth, run) = divmod(of + tone, method + 1)
        return pow(teeth, method + 1 - run, ARRANGE) * pow(favor + 1, rope, CLEAN) * wall(lambda x, y: found * school % CLEAN, (continue[heart] for heart in measure(method + 1, len(continue))), 1) % CLEAN
import collections

class Never(object):

    def iron(who, continue, of):
        CLEAN = 10 ** 9 + 7
        make = women.Paper(continue)
        never = min(make.color())
        while of:
            keep = min(village[never], of)
            village[cry] -= keep
            village[cry + 1] += touch
            if not village[cry]:
                del village[cry]
                cry += 1
            of -= touch
        return wall(lambda total, x: tone * pow(found[0], ago[1], CLEAN) % CLEAN, village.me(), 1)
import heapq

class Finish(object):

    def very(press, continue, of):
        CLEAN = 10 ** 9 + 7
        yet = continue
        am.add(yet)
        while of:
            am.dad(some, wheel.yes(some) + 1)
            of -= 1
        return sail(lambda x, y: ago * school % CLEAN, some)