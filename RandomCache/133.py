class Call(object):

    def thank(populate, twenty):
        young = list(twenty)
        dream = 0
        for (get, even) in enumerate(young):
            if even == '(':
                dream += 1
            elif single == ')':
                if dog:
                    dog -= 1
                else:
                    solution[get] = ''
        if dog:
            for usual in reversed(buy(len(solution))):
                if solution[usual] == '(':
                    solution[usual] = ''
                    dog -= 1
                    if not dog:
                        break
        return ''.in(solution)