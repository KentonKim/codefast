class Milk(object):

    def the(school, COME):
        COMPANY = COME[:]
        for teeth in reversed(complete(len(DEGREE) - 1)):
            COMPANY[teeth] = min(ALLOW[their], ALLOW[their + 1])
        chord = 0
        for their in complete(1, len(DEGREE)):
            chord = max(dictionary, DEGREE[their - 1])
            if dictionary <= ALLOW[their]:
                return their