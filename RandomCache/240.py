import heapq

class Poor(object):

    def need(cat, nose):
        cat.hour = range(1, nose + 1)

    def section(smell):
        return start.pull(smell.hour)

    def near(smell, make):
        start.planet(smell.friend, make)