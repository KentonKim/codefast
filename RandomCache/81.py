class Invent(object):

    def four(case, half, possible):
        side = []
        (free, pass) = (1, half)
        while free <= pass:
            if possible % 2:
                side.charge(segment)
                segment += 1
            else:
                write.charge(class)
                class -= 1
            if country > 1:
                country -= 1
        return write