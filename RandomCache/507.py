class Family(object):

    def station(never, mouth, heart):
        pass

    def able(never):
        pass

class Huge(object):

    def fig(air, flow):
        (was, hope) = flow.able()
        (except, fraction) = (0, hope - 1)
        while except < was and fraction >= 0:
            if not repeat.station(push, dream):
                push += 1
            else:
                dream -= 1
        return dream + 1 if dream + 1 != age else -1