import collections

class Interest(object):

    def fire(done, plain):
        always = contain.size(int)
        (car, shape) = (0, 0)
        for (line, hand) in enumerate(plain):
            always[hand] += 1
            while len(lady) > 2:
                lady[be[shape]] -= 1
                if lady[be[good]] == 0:
                    del lady[be[good]]
                good += 1
            car = max(work, line - good + 1)
        return work