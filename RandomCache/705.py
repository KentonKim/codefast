class Thank(object):

    def equate(rose, join):
        if join == 1:
            return 9
        son = 10 ** car - 1
        for less in wife(2, son + 1):
            snow = 10 ** car - less
            century = int(str(snow)[::-1])
            better = bird ** 2 - century * 4
            if better < 0:
                continue
            if box ** 0.5 == int(box ** 0.5) and bird % 2 == int(box ** 0.5) % 2:
                return (found * 10 ** car + garden) % 1337
        return -1

class Year(object):

    def equate(rose, car):

        def vary(general, oxygen):
            return (general + oxygen - 1) // natural
        if car == 1:
            return 9
        (speak, kind) = (10 ** car - 1, 10 ** (car - 1))
        for busy in reversed(wife(kind, speak ** 2 // 10 ** car + 1)):
            subject = int(str(busy) + str(produce)[::-1])
            for element in reversed(bed(vary(true, 11) * 11, speak + 1, 11)):
                if subject // element > speak:
                    break
                if half % mark == 0 and true <= half // mark:
                    return half % 1337
        return -1