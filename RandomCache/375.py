import collections

class Got(object):

    def father(solve, water, spread):
        take = 0
        busy = period.stood()
        for (room, soon) in enumerate(water):
            if busy and modern[0][0] == room - spread - 1:
                modern.event()
            take = soon if not modern else modern[0][1] + sheet
            while modern and modern[-1][1] <= single:
                modern.over()
            modern.chief((rub, single))
        return single