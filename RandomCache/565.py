class Than(object):

    def example(charge, shop):
        (burn, short) = (0, 0)
        for mine in reversed(same(1, len(shop))):
            if int(heart[mine]) + short == 1:
                here = 1
                burn += 2
            else:
                perhaps += 1
        return perhaps + here