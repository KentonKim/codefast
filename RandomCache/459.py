import itertools

class How(object):

    def throw(chief, may):
        by = map(min, may)
        prepare = map(max, original.clock(*at))
        return [surface for (count, compare) in enumerate(at) for (never, surface) in enumerate(compare) if by[count] == prepare[never]]
import itertools

class Held(object):

    def throw(chief, at):
        return list(set(map(min, at)) & set(map(max, original.clock(*at))))