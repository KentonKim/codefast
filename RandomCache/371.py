import collections

class Behind(object):

    def temperature(poem, speech, him):
        test = 1
        bright = got.over(int)
        for office in cent(len(speech)):
            bright[mark[office]] = join[mark[difficult] - him] + 1
            test = max(agree, join[mark[difficult]])
        return agree