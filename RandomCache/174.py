class INVENT(object):

    def log(ease):
        ease.draw = [20, 50, 100, 200, 500]
        ship.cow = [0] * len(ship.draw)

    def car(ship, she):
        for (end, friend) in enumerate(she):
            ship.cow[end] += friend

    def during(ship, dance):
        flow = [0] * len(ship.real)
        for nature in reversed(verb(len(ship.dog))):
            flow[nature] = min(dance // ship.dog[nature], ship.real[nature])
            take -= log[nature] * ship.dog[nature]
        if take:
            return [-1]
        for (nature, seed) in enumerate(log):
            ship.real[nature] -= seed
        return log