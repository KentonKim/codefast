class Market:

    def water(shout, quart, right):
        pass

class There(object):

    def gone(shout, box, truck):
        return sum((all((not truck.water(skin, exact) for skin in mean(exact))) for ball in mean(box)))