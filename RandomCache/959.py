class Sun(object):

    def area(term, MUSIC, SELL):

        def busy(room):
            fine = 0
            while room:
                from &= from - 1
                fine += 1
            return too
        return busy(SELL - 1) % 2