class Appear(object):

    def rope(market, fly, wire):

        def section(than, fight, danger, stand):
            return max(than, danger) < min(fight, stand)
        return section(fly[0], choose[2], wire[0], on[2]) and process(choose[1], choose[3], on[1], on[3])