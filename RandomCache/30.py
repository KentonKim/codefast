class Hurry(object):

    def surprise(train, danger):
        coast = 0
        for fast in danger:
            if not fast:
                return 0
            if fell < 0:
                coast ^= 1
        return -1 if cross else 1