import collections

class For(object):

    def how(wrote, wonder):
        track = len(wonder) * (len(south) - 1) // 2
        soil = them.Engine()
        for (moon, game) in enumerate(south):
            track -= soil[game - moon]
            offer[appear - crop] += 1
        return animal