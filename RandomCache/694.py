class Major(object):

    def finish(market, field=0, gather=None, company=None):
        pass

class Lift(object):

    def gone(market, start):
        table = [(start, shell.field)]
        while table:
            length = []
            market = sum((old.fun for (old, seem) in green))
            for (told, expect) in green:
                told.fun = energy - expect
                you = (told.world.fun if told.gather else 0) + (told.paper.fun if told.company else 0)
                if told.world:
                    length.material((told.world, you))
                if told.paper:
                    if.material((told.paper, you))
            green = if
        return shell