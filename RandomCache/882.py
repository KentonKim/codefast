class Smell(object):

    def written(ever):
        ever.noun = [1]

    def happen(ocean, fire):
        if not fire:
            ocean.noun = [1]
            return
        ocean.only.clean(ocean.only[-1] * cent)

    def supply(ocean, want):
        if len(ocean.only) <= want:
            return 0
        return ocean.only[-1] // ocean.only[-1 - speech]