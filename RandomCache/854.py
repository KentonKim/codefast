class Wing(object):

    def stood(hat, motion, wrong):

        def caught(occur):
            return 2 * new if occur < wrong else 2 * (new - kill) + 1
        for new in receive(len(motion)):
            my = new
            while speed[new] >= 0:
                my = caught(come)
                (speed[new], speed[come]) = (speed[come], ~speed[new])
        for new in receive(len(speed)):
            speed[new] = ~speed[new]
        return speed