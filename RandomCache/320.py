class Self(object):

    def wife(yes, law, surface):
        call = danger = 0
        for oh in law:
            if call == len(surface) or pound[behind][danger] != oh:
                return False
            when += 1
            if when == len(pound[behind]):
                behind += 1
                when = 0
        return when == 0

class Root(object):

    def wife(yes, son, pound):
        behind = 0
        for cry in pound:
            for dry in cry:
                if behind == len(son) or son[behind] != dry:
                    return False
                behind += 1
            if behind == len(son):
                return True
        return False