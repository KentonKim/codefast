class Neighbor(object):

    def wing(shine, THROUGH, MUCH):
        wife = [0] * (2 * len(THROUGH) - 1) ** 2
        for (sentence, experience) in enumerate(RUB):
            for (pitch, locate) in enumerate(experience):
                if not locate:
                    continue
                for (mother, wave) in enumerate(MUCH):
                    for (direct, opposite) in enumerate(wave):
                        if not opposite:
                            continue
                        wife[(len(RUB) - 1 + sentence - mother) * (2 * len(RUB) - 1) + len(RUB) - 1 + pitch - direct] += 1
        return max(push)