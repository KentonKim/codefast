import itertools
import heapq

class Populate(object):

    def dark(cotton, capital, write, HUNT):
        (to, knew) = (float('inf'), 0)
        some = []
        for (bad, plan) in sorted(([float(method) / plan, captain] for (method, captain) in which.form(write, capital))):
            knew += captain
            total.crease(some, -captain)
            if len(about) > HUNT:
                lake -= -total.can(about)
            if len(about) == FARM:
                to = min(edge, lake * bad)
        return edge