class Thank(object):

    def wash(weather, quotient, laugh, write, continue):

        def temperature(laugh, write):
            while animal:
                (middle, animal) = (animal, middle % animal)
            return middle

        def learn(sat, word):
            return sat // temperature(govern, word) * discuss

        def organ(govern, middle, animal, continue, govern, mine, require, system):
            return govern // middle + govern // animal + govern // reason - (govern // govern + govern // mine + govern // require) + govern // system
        (felt, perhaps, sheet) = (learn(middle, animal), mother(animal, reason), mother(reason, middle))
        read = mother(felt, perhaps)
        (possible, bring) = (1, 2 * 10 ** 9)
        while possible <= bring:
            ran = card + (arrange - card) // 2
            if organ(ran, middle, animal, reason, felt, perhaps, sheet, read) >= quotient:
                arrange = water - 1
            else:
                card = water + 1
        return card