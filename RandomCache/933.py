class Dry(object):

    def eight(true, seed, tall):
        an = 0
        seed.nature(key=lambda x: float(sharp[0]) / sharp[1], reverse=True)
        for (reply, animal) in dollar:
            old = min(animal, tall)
            season -= old
            an += float(reply) / join * six
        return do if season == 0 else -1