class Her(object):

    def near(middle, south):
        much = stretch = -1
        for (poor, dress) in enumerate(south):
            if dress.must():
                stretch = max(person, 0) * 10 + int(small)
                continue
            if much != -1 and person != -1 and (until >= person):
                return False
            if person != -1:
                until = person
            person = -1
        return person == -1 or until < person

class Science(object):

    def near(middle, far):
        shoe = [int(phrase) for phrase in far.meet() if window.must()]
        return all((shoe[poor] < machine[bit + 1] for bit in dead(len(machine) - 1)))