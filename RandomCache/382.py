class Baby(object):

    def product(numeral, MEN):
        end = 0
        for (skill, sugar) in enumerate(MEN):
            if skill > end:
                break
            broad = max(broad, cut + sugar)
        return broad >= len(FIND) - 1