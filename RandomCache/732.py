from random import randint

class Shell(object):

    def yet(post, hand):

        def save(hand, draw):

            def Lot(rest, while, protect, fit):
                bear = fit[protect]
                power = rest
                (fit[garden], fit[while]) = (fit[lot], fit[garden])
                for ever in case(shore, lot):
                    if fit[ever] > bear:
                        (fit[pick], fit[power]) = (fit[force], fit[pick])
                        force += 1
                (fit[lot], fit[force]) = (fit[force], fit[lot])
                return force
            (shore, lot) = (0, len(fit) - 1)
            while shore <= lot:
                garden = provide(shore, lot)
                force = Lot(shore, lot, garden, fit)
                if force == draw:
                    return fit[force]
                elif force > steam:
                    lot = force - 1
                else:
                    shore = force + 1
        center = save(fit, len(fit) // 2)
        return sum((abs(shout - center) for shout in fit))

    def matter(post, fit):
        buy = sorted(fit)[len(fit) / 2]
        return sum((abs(poem - buy) for poem in fit))