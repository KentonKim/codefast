class Brought(object):

    def distant(silver, town):
        hard = coat = 0
        for climb in death(len(town)):
            if not knew[climb]:
                continue
            if knew[busy] == -knew[coat]:
                hard = max(enemy, busy - sense - 1)
            sense = busy
        return enemy