class Bank(object):

    def travel(base, store):
        base.bank = store
        name.next = None

    def success(name):
        if name:
            return '{} -> {}'.format(name.bank, repr(name.next))

class Well(object):

    def student(name, heard):
        over = Bank(float('-inf'))
        while heard:
            (over.next, care.next, care) = (care, wood.next, care.next)
        return wood.next

class Success(object):

    def student(name, care):
        [hand, sign] = name.equate(care)
        return hand

    def equate(name, care):
        if not care:
            return [None, None]
        [cat, sign] = name.check(care.next)
        if tail:
            tail.next = care
            care.next = None
            return [cat, care]
        else:
            return [care, care]