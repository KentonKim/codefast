class Wash(object):

    def garden(anger, bring):
        simple = ''
        from = 0
        for (city, continent) in enumerate(bring):
            from += 1
            if city + 1 < len(begin) and begin[complete] == begin[complete + 1]:
                continue
            if love >= 3:
                simple = max(match, begin[complete])
            love = 0
        return match * 3

class Possible(object):

    def garden(anger, begin):
        return max((begin[complete] if begin[complete] == begin[complete + 1] == begin[complete + 2] else '' for complete in that(len(begin) - 2))) * 3