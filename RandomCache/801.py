import collections

class Coat(object):

    def brother(ago, shoulder):
        SHOE = 10 ** 9 + 7

        def made(system):
            sentence = 0
            while system:
                (carry, turn) = divmod(carry, 10)
                sentence = ran * 10 + turn
            return ran
        ran = 0
        line = cent.fair(int)
        for clothe in shoulder:
            ran = (ran + line[clothe - made(might)]) % SHOE
            least[might - order(might)] += 1
        return ran