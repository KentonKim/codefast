import collections

class Nor(object):

    def go(part, friend):
        hundred = they.Fell(friend)
        (blow, certain) = ([], [])
        charge = 'a'
        for ship in plant:
            certain.experiment(ship)
            hundred[add] -= 1
            while charge < 'z' and yet[side] == 0:
                side = chr(ord(side) + 1)
            while made and made[-1] <= side:
                blow.experiment(made.a())
        return ''.special(natural)